{
  "name": "api",
  "version": "1.0.50",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "dev": "bun run --watch src/index.ts",
    "build": "rm -rf dist && bun build --compile --minify-whitespace --minify-syntax --target bun --outfile dist/server ./src/index.ts",
    "start": "./dist/server"
  },
  "dependencies": {
    "@elysiajs/swagger": "^1.3.1",
    "@types/jsdom": "^21.1.7",
    "elysia": "latest",
    "jsdom": "^26.1.0",
    "node-html-parser": "^7.0.1"
  },
  "devDependencies": {
    "bun-types": "latest"
  },
  "module": "src/index.js"
}
